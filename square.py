import pygame
from pieces import *

class square:

    def __init__(self, tile_size, color) -> None:
        self.color = 'color'
    """
    def draw_self(self, window):
        pygame.draw.rect(window, color, (col * self.tile_size, row * self.tile_size, self.tile_size, self.tile_size))
    """